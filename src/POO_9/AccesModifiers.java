package POO_9;

public class AccesModifiers {
	public static void main(String[] args) {
		
		/*Encapsulamiento (modificadores de acceso)
		 * - public = se accede desde cualquier parte del programa
		 * - private = solo desde la clase
		 * - protected = desde la misma calse y subclases
		 * - default = solo en el mismo paquete
		 */
		
	}
}
